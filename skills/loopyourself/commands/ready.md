---
description: USER ONLY — admit Backlog issue(s) into the workflow (Active pool, status Ready).
---

This command represents the user's review decision. Ask which issue(s) to admit if unclear, then run `loopyourself ready <LY-001>` (or `all`). Never suggest running this on the agent's own initiative during a loop.
