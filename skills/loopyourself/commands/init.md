---
description: Initialize LoopYourself in this repository — creates .loopyourself/, detects AGENTS.md conventions, optionally links Linear.
---

Run `npx loopyourself init` (or `loopyourself init` if installed) and show its output.

If the user mentions a Linear team, also run `loopyourself link <team> [project]` and remind them to fill `statusMap` in `.loopyourself/config.json` (required before any Linear push).
