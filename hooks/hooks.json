{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__linear.*",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const i=JSON.parse(require('fs').readFileSync(0,'utf8'));const n=i.tool_name||'';if(/delete|trash|archive|destroy|remove/i.test(n)){console.error('LoopYourself: refusing destructive Linear MCP tool: '+n);process.exit(2)}\""
          }
        ]
      }
    ]
  }
}
